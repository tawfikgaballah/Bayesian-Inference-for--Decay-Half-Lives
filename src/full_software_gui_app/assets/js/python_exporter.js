(function () {
  function pyJsonString(value) {
    return JSON.stringify(JSON.stringify(value, null, 2));
  }

  function safeName(value) {
    return String(value || "nucleus").replace(/[^A-Za-z0-9_-]+/g, "_") || "nucleus";
  }

  function exportedPythonScript(payload) {
    const configJson = pyJsonString(payload.config || {});
    const fitRowsJson = pyJsonString(payload.fit_rows || []);
    const designJson = pyJsonString(payload.design || {});
    const nucleus = payload.config?.parent_nucleus || "nucleus";
    const scriptName = `${safeName(nucleus)}_bayesian_run.py`;
    const generatedAt = new Date().toISOString();

    return `#!/usr/bin/env python3
"""Run the exported beta-decay half-life Bayesian model.

This file was generated by the Full Software Decay GUI on ${generatedAt}.
It contains the selected fit data, prior design, PyMC model construction,
Bateman model calls, posterior sampling, PPC sampling, and result saving.

Typical use from the standalone package folder:
    PYTHONPATH=src .venv/bin/python /path/to/${scriptName}

Optional:
    PYTHONPATH=src .venv/bin/python /path/to/${scriptName} --results-dir ./my_results
"""

from __future__ import annotations

import argparse
import inspect
import json
import sys
import threading
import time
import traceback
import uuid
from pathlib import Path


CONFIG = json.loads(${configJson})
FIT_ROWS = json.loads(${fitRowsJson})
DESIGN = json.loads(${designJson})

az = None
np = None
pm = None
pt = None
parent_mod = None
daughter_mod = None
granddaughter_mod = None
int_exp = None
build_and_save_results = None
_RUN_STARTED = time.monotonic()


def log(message: str) -> None:
    elapsed = time.monotonic() - _RUN_STARTED
    print(f"{elapsed:7.1f}s  {message}", flush=True)


def heartbeat(label: str, interval: float = 15.0):
    state = {"running": True, "started": time.monotonic()}

    def beat():
        while state["running"]:
            time.sleep(interval)
            if state["running"]:
                elapsed = time.monotonic() - state["started"]
                log(f"{label} still running ({elapsed:.1f}s elapsed).")

    thread = threading.Thread(target=beat, daemon=True)
    thread.start()

    def stop():
        state["running"] = False

    return stop


def add_local_package_path() -> None:
    """Find the standalone package source tree when it is not installed."""
    here = Path(__file__).resolve().parent
    candidates = [
        here / "src",
        here.parent / "src",
        Path.cwd() / "src",
        Path.cwd() / "standalone_decay_gui" / "src",
        here / "standalone_decay_gui" / "src",
        here.parent / "standalone_decay_gui" / "src",
    ]
    for candidate in candidates:
        if (candidate / "full_software_gui_app").is_dir():
            sys.path.insert(0, str(candidate))
            return


def import_analysis_stack() -> bool:
    """Import PyMC and the package helpers used by this generated model."""
    global az, np, pm, pt
    global parent_mod, daughter_mod, granddaughter_mod, int_exp
    global build_and_save_results

    add_local_package_path()
    try:
        import arviz as _az
        import numpy as _np
        import pymc as _pm
        import pytensor.tensor as _pt
        from full_software_gui_app.bateman import (
            daughter_mod as _daughter_mod,
            granddaughter_mod as _granddaughter_mod,
            int_exp as _int_exp,
            parent_mod as _parent_mod,
        )
        from full_software_gui_app.results import build_and_save_results as _build_and_save_results
    except ImportError as exc:
        print("Could not import the Bayesian analysis package.", file=sys.stderr)
        print("Run from standalone_decay_gui or install the package first:", file=sys.stderr)
        print("    PYTHONPATH=src .venv/bin/python " + Path(__file__).name, file=sys.stderr)
        print("Import error: " + str(exc), file=sys.stderr)
        return False

    az = _az
    np = _np
    pm = _pm
    pt = _pt
    parent_mod = _parent_mod
    daughter_mod = _daughter_mod
    granddaughter_mod = _granddaughter_mod
    int_exp = _int_exp
    build_and_save_results = _build_and_save_results
    return True


def current_payload() -> dict:
    return {"config": CONFIG, "fit_rows": FIT_ROWS, "design": DESIGN}


def neutron_sort_key(mode: str) -> int:
    return int(str(mode).replace("n", ""))


def fit_arrays():
    x = np.array([float(row["BinCenter"]) for row in FIT_ROWS], dtype=float)
    binw = np.array([float(row["BinWidth"]) for row in FIT_ROWS], dtype=float)
    y = np.array([float(row["BinContent"]) for row in FIT_ROWS], dtype=float)
    if len(x) == 0:
        raise ValueError("No fit rows are embedded in this exported analysis script.")
    return x, binw, y


def make_prior(name: str, spec: dict, positive: bool = False):
    """Create one PyMC variable from the exported prior specification."""
    dist = str(spec.get("dist", "normal")).lower()

    if dist == "fixed":
        value = pt.as_tensor_variable(float(spec.get("value", spec.get("mu", 1.0))))
        return pm.Deterministic(name, value)

    if dist == "normal":
        rv = pm.Normal(
            name,
            mu=float(spec.get("mu", 1.0)),
            sigma=max(float(spec.get("sigma", 1.0)), 1e-12),
        )
    elif dist == "uniform":
        rv = pm.Uniform(
            name,
            lower=float(spec.get("lower", 0.0)),
            upper=float(spec.get("upper", 1.0)),
        )
    elif dist == "halfnormal":
        rv = pm.HalfNormal(
            name,
            sigma=max(float(spec.get("sigma", 1.0)), 1e-12),
        )
    elif dist == "lognormal":
        scale = str(spec.get("scale", "natural")).lower()
        if scale == "log":
            log_mu = float(spec.get("mu", 0.0))
            log_sigma = float(spec.get("sigma", 1.0))
            natural_mu = float(np.exp(np.clip(log_mu, -700, 700)))
        else:
            natural_mu = float(spec.get("mu", 1.0))
            natural_sigma = float(spec.get("sigma", 1.0))
            if natural_mu <= 0:
                raise ValueError(name + " lognormal mu must be > 0 before log conversion.")
            if natural_sigma <= 0:
                raise ValueError(name + " lognormal sigma must be > 0 before log conversion.")
            log_mu = float(np.log(natural_mu))
            log_sigma = float(np.log1p(natural_sigma / natural_mu))
        if log_sigma <= 0:
            raise ValueError(name + " lognormal sigma must be > 0 after log conversion.")
        if name == "A0":
            log(
                "A0 LogNormal prior: "
                + f"natural_mu={natural_mu:.6g}, "
                + f"pymc_mu={log_mu:.6g}, "
                + f"pymc_sigma={log_sigma:.6g}"
            )
        rv = pm.LogNormal(
            name,
            mu=log_mu,
            sigma=max(log_sigma, 1e-12),
            initval=max(natural_mu, 1e-12),
        )
    else:
        raise ValueError("Unknown prior distribution: " + dist)

    if positive:
        return pm.Deterministic(name + "_positive", pt.maximum(rv, 1e-12))
    return rv


def build_bayesian_model(x, binw, y):
    """Build the exported PyMC model from CONFIG and DESIGN."""
    priors = DESIGN.get("priors", {})
    log(
        "Building PyMC model "
        + f"(likelihood={CONFIG.get('likelihood', 'poisson')}, "
        + f"background={CONFIG.get('background_type', 'constant')}, "
        + f"branching={DESIGN.get('branching_mode', 'fixed')})."
    )

    with pm.Model() as model:
        a0 = make_prior(
            "A0",
            priors.get(
                "A0",
                {
                    "dist": "lognormal",
                    "mu": max(float(CONFIG.get("A0_estimate", 1.0)), 1e-12),
                    "sigma": max(float(CONFIG.get("A0_estimate", 1.0)), 1e-12),
                },
            ),
            positive=True,
        )
        t_parent = make_prior(
            "T_parent",
            priors.get(
                "T_parent",
                {
                    "dist": "normal",
                    "mu": CONFIG["t_parent"],
                    "sigma": CONFIG["t_parent"],
                },
            ),
            positive=True,
        )

        daughter_halflives = {}
        daughter_uncertainties = CONFIG.get("daughter_halflife_uncertainties", {})
        for mode, value in CONFIG.get("daughter_halflives", {}).items():
            sigma = daughter_uncertainties.get(mode, value)
            daughter_halflives[mode] = make_prior(
                "T_" + mode,
                priors.get("T_" + mode, {"dist": "normal", "mu": value, "sigma": sigma}),
                positive=True,
            )

        granddaughter_halflives = {}
        granddaughter_uncertainties = CONFIG.get("granddaughter_halflife_uncertainties", {})
        for mode, value in CONFIG.get("granddaughter_halflives", {}).items():
            sigma = granddaughter_uncertainties.get(mode, value)
            granddaughter_halflives[mode] = make_prior(
                "T_g" + mode,
                priors.get("T_g" + mode, {"dist": "normal", "mu": value, "sigma": sigma}),
                positive=True,
            )

        daughter_modes = sorted(daughter_halflives.keys(), key=neutron_sort_key)
        n_br = len(daughter_modes)
        branching_mode = str(DESIGN.get("branching_mode", "fixed")).lower()
        daughter_branches = {}

        if n_br:
            if branching_mode == "fixed":
                fixed = DESIGN.get("fixed_daughter_branches") or CONFIG.get("daughter_branches", {})
                for mode in daughter_modes:
                    daughter_branches[mode] = pt.as_tensor_variable(float(fixed.get(mode, 0.0)))
                    pm.Deterministic("b_" + mode, daughter_branches[mode])

            elif branching_mode == "dirichlet":
                alpha = np.asarray(priors.get("b_dirichlet_alpha", [1.0] * n_br), dtype=float)
                if alpha.size != n_br:
                    alpha = np.pad(alpha, (0, max(0, n_br - alpha.size)), constant_values=1.0)[:n_br]
                b = pm.Dirichlet("b", a=alpha)
                for i, mode in enumerate(daughter_modes):
                    daughter_branches[mode] = b[i]
                    pm.Deterministic("b_" + mode, b[i])

            elif branching_mode == "softmax":
                loc = np.asarray(priors.get("b_softmax_loc", [0.0] * n_br), dtype=float)
                if loc.size != n_br:
                    loc = np.pad(loc, (0, max(0, n_br - loc.size)), constant_values=0.0)[:n_br]
                sig = max(float(priors.get("b_softmax_sigma", 1.0)), 1e-12)
                logits = pm.Normal("logits", mu=loc, sigma=sig, shape=n_br)
                exp_logits = pt.exp(logits - pt.max(logits))
                b = pm.Deterministic("b", exp_logits / pt.sum(exp_logits))
                for i, mode in enumerate(daughter_modes):
                    daughter_branches[mode] = b[i]
                    pm.Deterministic("b_" + mode, b[i])

            elif branching_mode == "normalized_raw":
                raw = pt.stack([
                    make_prior(
                        "b_raw" + str(i),
                        priors.get("b_raw" + str(i), {"dist": "halfnormal", "sigma": 1.0}),
                        positive=True,
                    )
                    for i in range(n_br)
                ])
                b = pm.Deterministic("b", raw / pt.sum(raw))
                for i, mode in enumerate(daughter_modes):
                    daughter_branches[mode] = b[i]
                    pm.Deterministic("b_" + mode, b[i])

            else:
                raise ValueError("Unknown branching mode: " + branching_mode)

        granddaughter_branches = {}
        gd_specs = DESIGN.get("granddaughter_branch_specs", {})
        for mode in sorted(granddaughter_halflives.keys(), key=neutron_sort_key):
            spec = gd_specs.get(mode, {"dist": "fixed", "value": 1.0})
            dist = str(spec.get("dist", "fixed")).lower()
            if dist == "fixed":
                value = float(spec.get("value", 1.0))
                if not 0 <= value <= 1:
                    raise ValueError("Granddaughter branch " + mode + " fixed value must be between 0 and 1.")
                granddaughter_branches[mode] = pt.as_tensor_variable(value)
                pm.Deterministic("bgd_" + mode, granddaughter_branches[mode])
            elif dist == "uniform":
                lower = float(spec.get("lower", 0.0))
                upper = float(spec.get("upper", 1.0))
                if lower < 0 or upper > 1 or lower >= upper:
                    raise ValueError("Granddaughter branch " + mode + " uniform range must satisfy 0 <= lower < upper <= 1.")
                granddaughter_branches[mode] = pm.Uniform("bgd_" + mode, lower=lower, upper=upper)
            else:
                raise ValueError("Unknown granddaughter branch prior for " + mode + ": " + dist)

        # Bateman decay-chain model.
        mu = parent_mod(np, pt, x, binw, t_parent, a0)
        for mode in daughter_modes:
            t_d = daughter_halflives[mode]
            b_pd = daughter_branches[mode]
            mu = mu + daughter_mod(np, pt, x, binw, t_d, t_parent, a0, b_pd)
            if mode in granddaughter_halflives:
                b_dg = granddaughter_branches.get(mode, pt.as_tensor_variable(1.0))
                mu = mu + granddaughter_mod(
                    np,
                    pt,
                    x,
                    binw,
                    granddaughter_halflives[mode],
                    t_d,
                    t_parent,
                    a0,
                    b_pd,
                    b_dg,
                )

        bg_type = CONFIG.get("background_type", "constant")
        bg_params = CONFIG.get("background_params", {})
        if bg_type == "constant":
            bkg = make_prior(
                "bkg_rate",
                priors.get(
                    "bkg_rate",
                    {
                        "dist": "normal",
                        "mu": bg_params.get("bkg_rate", 0.0),
                        "sigma": max(bg_params.get("bkg_rate", 1.0), 1e-6),
                    },
                ),
                positive=True,
            )
            mu = mu + bkg * binw
        elif bg_type == "linear":
            bkg0 = make_prior(
                "bkg0",
                priors.get("bkg0", {"dist": "normal", "mu": bg_params.get("bkg0", 0.0), "sigma": 1.0}),
                positive=True,
            )
            slope = make_prior(
                "slope",
                priors.get("slope", {"dist": "normal", "mu": bg_params.get("slope", 0.0), "sigma": 0.01}),
            )
            mu = mu + (bkg0 + slope * x) * binw
        elif bg_type == "exponential":
            bg_amp = make_prior(
                "bg_amp",
                priors.get("bg_amp", {"dist": "normal", "mu": bg_params.get("bg_amp", 0.0), "sigma": 1.0}),
                positive=True,
            )
            bg_half = make_prior(
                "bg_halflife",
                priors.get("bg_halflife", {"dist": "normal", "mu": bg_params.get("bg_halflife", 1.0), "sigma": 1.0}),
                positive=True,
            )
            lam_bg = np.log(2.0) / bg_half
            mu = mu + bg_amp * int_exp(pt, lam_bg, x - 0.5 * binw, x + 0.5 * binw)

        mu = pt.clip(mu, 1e-12, 1e100)
        if CONFIG.get("likelihood", "poisson") == "normal":
            sigma_y = make_prior(
                "sigma_y",
                priors.get("sigma_y", {"dist": "halfnormal", "sigma": max(float(np.std(y)), 1.0)}),
                positive=True,
            )
            pm.Normal("obs", mu=mu, sigma=sigma_y, observed=y)
        else:
            pm.Poisson("obs", mu=mu, observed=y)

    return model


def sample_posterior(model):
    sampling = DESIGN.get("sampling", {})
    draws = int(sampling.get("draws", 1000))
    tune = int(sampling.get("tune", 1000))
    chains = int(sampling.get("chains", 4))
    cores = int(sampling.get("cores", 1))
    target_accept = float(sampling.get("target_accept", 0.9))
    log(
        "Sampling posterior: "
        + f"{chains} chain(s), {tune:,} tune + {draws:,} draws each, "
        + f"cores={cores}, target_accept={target_accept:g}."
    )

    sample_progress = {"last_log": 0.0}

    def sample_callback(trace, draw):
        try:
            now = time.monotonic()
            draw_idx = int(getattr(draw, "draw_idx", 0))
            chain_idx = getattr(draw, "chain", "?")
            tuning = bool(getattr(draw, "tuning", False))
            phase = "tuning" if tuning else "posterior"
            if now - sample_progress["last_log"] < 3.0 and draw_idx not in {0, tune, tune + draws - 1}:
                return
            sample_progress["last_log"] = now
            log(f"Sampling progress: chain {chain_idx}, {phase}, draw index {draw_idx:,}.")
        except Exception:
            pass

    sample_kwargs = {
        "draws": draws,
        "tune": tune,
        "chains": chains,
        "cores": cores,
        "target_accept": target_accept,
        "return_inferencedata": True,
        "progressbar": False,
        "compute_convergence_checks": False,
    }
    if "callback" in inspect.signature(pm.sample).parameters:
        sample_kwargs["callback"] = sample_callback

    sample_started = time.monotonic()
    stop_heartbeat = heartbeat("Sampling posterior")
    try:
        with model:
            idata = pm.sample(**sample_kwargs)
    finally:
        stop_heartbeat()
    log(f"Posterior sampling finished in {time.monotonic() - sample_started:.1f}s.")
    return idata


def sample_ppc(model, idata):
    try:
        chain_count = int(idata.posterior.sizes.get("chain", 1))
        draw_count = int(idata.posterior.sizes.get("draw", 1))
        max_ppc_draws = 800
        draws_per_chain = max(1, min(draw_count, int(np.ceil(max_ppc_draws / max(chain_count, 1)))))
        draw_indices = np.unique(np.linspace(0, draw_count - 1, draws_per_chain, dtype=int))
        posterior_subset = idata.posterior.isel(draw=draw_indices)
        ppc_trace = az.InferenceData(posterior=posterior_subset)
        log(
            "Sampling posterior predictive: "
            + f"{chain_count * len(draw_indices):,} posterior draw(s) used for PPC plot."
        )
        ppc_started = time.monotonic()
        stop_ppc_heartbeat = heartbeat("Posterior predictive sampling")
        try:
            with model:
                ppc_data = pm.sample_posterior_predictive(
                    ppc_trace,
                    var_names=["obs"],
                    return_inferencedata=False,
                    progressbar=False,
                )
        finally:
            stop_ppc_heartbeat()
        log(f"Posterior predictive sampling finished in {time.monotonic() - ppc_started:.1f}s.")
        return np.asarray(ppc_data.get("obs")) if "obs" in ppc_data else None
    except Exception as exc:
        log("Posterior predictive plot will be skipped: " + str(exc))
        return None


def run_analysis(args) -> dict:
    unit = CONFIG.get("output_unit", "unit")
    nucleus = CONFIG.get("parent_nucleus", "Nucleus")
    x, binw, y = fit_arrays()
    log(
        "Loaded fit data: "
        + f"{len(x):,} bins, "
        + f"time {float(np.min(x)):.6g} to {float(np.max(x)):.6g} {unit}, "
        + f"total counts {float(np.sum(y)):.6g}."
    )

    model = build_bayesian_model(x, binw, y)
    idata = sample_posterior(model)
    ppc_obs = sample_ppc(model, idata)

    log("Preparing and saving result artifacts...")
    return build_and_save_results(
        job_id=args.job_id or str(uuid.uuid4()),
        payload=current_payload(),
        idata=idata,
        ppc_obs=ppc_obs,
        x=x,
        y=y,
        unit=unit,
        nucleus=nucleus,
        result_dir=args.results_dir,
        log=log,
        az=az,
        np=np,
    )


def main() -> int:
    parser = argparse.ArgumentParser(description="Run the exported beta-decay Bayesian model.")
    parser.add_argument("--results-dir", default=None, help="Directory where the full result folder will be saved.")
    parser.add_argument("--job-id", default=None, help="Optional job id used in the result folder name.")
    args = parser.parse_args()

    log("Importing Bayesian libraries and package helpers...")
    if not import_analysis_stack():
        return 2

    try:
        result = run_analysis(args)
    except Exception:
        print("\\nModel run failed.", file=sys.stderr)
        traceback.print_exc()
        return 1

    log("Complete.")
    print("\\nModel run complete.")
    if result.get("result_dir"):
        print("Result folder: " + str(result["result_dir"]))
    if result.get("inference_data_path"):
        print("InferenceData NetCDF: " + str(result["inference_data_path"]))
    if result.get("saved_files"):
        print("Saved files: " + str(len(result["saved_files"])))
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
`;
  }

  window.DecayPythonExporter = { exportedPythonScript };
})();
